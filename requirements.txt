# ROS Bag文件数据处理项目 - 依赖库
# 安装前请确保已安装Python 3.7+

# ====== 推荐方案：使用传统rosbag（需要完整ROS环境）======
# 这是ROS官方提供的标准接口，兼容性和稳定性最佳！
# 安装方法：
#   1. 确保已配置ROS环境（如Ubuntu + ROS Noetic）
#   2. source /opt/ros/noetic/setup.bash
#   3. pip install rosbag rospy

# ====== 基础依赖 (无需ROS) ======
numpy>=1.21.0
matplotlib>=3.5.0
scipy>=1.7.0
pandas>=1.3.0
PyQt5>=5.15.0
pyyaml>=6.0

# ====== 备用方案：使用rosbags（纯Python实现）======
# 如果没有ROS环境，可以使用这个纯Python库作为备选方案
# 注意：可能存在某些兼容性问题，建议优先使用传统rosbag
# rosbags>=3.0.0

# ====== ROS环境配置指南 ======
#
# 方案1: Ubuntu + ROS Noetic（推荐）
#   sudo apt-get update
#   sudo apt-get install ros-noetic-rosbag
#   source /opt/ros/noetic/setup.bash
#   python main.py
#
# 方案2: Ubuntu + ROS Melodic
#   sudo apt-get update
#   sudo apt-get install ros-melodic-rosbag
#   source /opt/ros/melodic/setup.bash
#   python main.py
#
# 方案3: Docker容器运行ROS
#   docker pull osrf/ros:noetic-desktop-full
#   docker run -it --rm -v $(pwd):/workspace osrf/ros:noetic-desktop-full
#   cd /workspace && python main.py
#
# 方案4: Windows WSL2 + ROS
#   在WSL2中安装Ubuntu并按照方案1配置ROS环境
